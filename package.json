{
  "name": "spamgram",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:migrate": "wrangler d1 migrations apply spamgram --local",
    "db:migrate:remote": "wrangler d1 migrations apply spamgram --remote",
    "webhook:set": "node scripts/set-webhook.mjs"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.9.14",
    "@types/node": "^26.5.1",
    "typescript": "^5.8",
    "vitest": "^3.2",
    "wrangler": "^4.131.1"
  }
}
